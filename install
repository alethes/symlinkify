DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
me="$(basename "$(test -L "$0" && readlink "$0" || echo "$0")")"
echo ${DIR}/linker
sudo rm /usr/bin/linker
sudo ln -s ${DIR}/linker /usr/bin/linker
linker